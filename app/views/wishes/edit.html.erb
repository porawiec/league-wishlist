<%= link_to "My Wishes", current_player %>
<%= link_to "Champions", champs_path %>
<%= link_to "Skins", skins_path %>
<%= link_to "Players", players_path %>
<%= link_to "Wishes", wishes_path %>
</br></br>
<%= link_to "Log out", logout_path, method: :delete %>

<h1>Wish Editing Page</h1>

<% if @errors %>
    <% @errors.each do |message| %>
        <p><%= message %></p> 
    <% end %>
<% end %>

<%= form_with model: @wish do |f| %>
    <%= f.label :rank %>
    <%= f.text_field :rank %></br>
    <%= f.collection_select :champ_id, Champ.all, :id, :name %><br>

    <%= f.submit "Edit"%>
<% end %>

    <p> <%= link_to "Delete this wish", wish_path(@wish), :data => {:confirm => "Are you sure?"}, :method => :delete %>